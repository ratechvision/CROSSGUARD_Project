project: CROSSGUARD

seed: 42
epochs: 30
batch_size: 128

learning_rate: 0.0001
weight_decay: 0.00001

num_classes: 15

sequence_length: 60
flow_features: 41

embedding_dim: 256

dropout: 0.2

device: auto

save_dir: artifacts

dataset:
  root: data/ids2018